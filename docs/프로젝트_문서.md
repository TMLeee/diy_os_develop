# DIY OS 프로젝트 문서

MINT64 OS를 기반으로 한 64비트 멀티태스크 운영체제 프로젝트의 설계 문서입니다.
BIOS가 로딩하는 512바이트 부트로더에서 시작해, 보호 모드(32비트)를 거쳐
최종적으로 **IA-32e(롱) 모드**의 64비트 커널까지 부팅합니다. 산출물은 1.44MB
플로피 이미지(`Disk.img`)이며 **QEMU**에서 실행합니다.

```
16비트 부트로더 ──► 32비트 보호 모드 커널 ──► 64비트 IA-32e 커널
 (Bootloader)          (Kernel32)                (Kernel64)
  섹터 로딩            메모리/CPU 확인,           GDT/IDT/TSS,
  0x7C00              페이지 테이블 생성          키보드/PIC/PIT/RTC,
                                                 콘솔 셸, 멀티태스킹
```

빌드/실행/디버깅 상세는 리포지터리 루트의 [README.md](../README.md)를 참고하세요.

---

## 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [프로젝트 구조](#2-프로젝트-구조)
3. [부팅 과정](#3-부팅-과정)
4. [메모리 구조](#4-메모리-구조)

---

## 1. 프로젝트 개요

### 무엇을 만드는가

- **MINT64 OS 기반의 64비트 운영체제**를 밑바닥부터 구현하는 학습용 프로젝트입니다.
- x86 CPU는 전원이 켜지면 16비트 리얼 모드로 시작하므로, 보호 모드(32비트)를
  거쳐 IA-32e(롱) 모드의 64비트 커널까지 단계적으로 부팅합니다.
- 산출물은 1.44MB 플로피 이미지(`Disk.img`)이며 QEMU에서 실행합니다.

### 현재 구현된 기능

| 분류 | 기능 |
| --- | --- |
| 부팅 | 16 → 32 → 64비트 모드 전환, 커널 이미지 디스크 로딩 |
| CPU/메모리 | CPUID로 64비트 지원 확인, 최소 64MB 메모리 검사, 총 RAM 크기 측정 |
| 세그먼트/인터럽트 | GDT, TSS, IDT 초기화 및 IST(인터럽트 전용 스택) 구성 |
| 인터럽트 처리 | PIC 컨트롤러 초기화·마스킹, ISR 핸들러, 예외 핸들러 |
| 입력 | PS/2 키보드 인터페이스, 키보드 큐, LED 제어 |
| 타이머/시계 | PIT(카운터), RTC(날짜·시간), TSC(타임스탬프 카운터) |
| 콘솔 | 텍스트 화면 출력(`kPrintf`), 커서 제어 |
| 셸 | 명령어 파싱 및 실행 콘솔 셸 |
| 멀티태스킹 | TCB 기반 태스크 컨텍스트 정의 및 컨텍스트 전환(`kSwitchContext`) |

### 콘솔 셸 명령어

`console_shell.c`의 명령어 테이블에 등록된 명령들입니다.

| 명령 | 설명 |
| --- | --- |
| `help` | 도움말 출력 |
| `cls` | 화면 지우기 |
| `totalram` | 총 RAM 크기 출력 |
| `strtod` | 문자열 → 10진수/16진수 변환 테스트 |
| `shutdown` | 종료 및 재부팅 |
| `settimer` | PIT 카운터0 설정 `settimer 10[ms] 1[periodic]` |
| `wait` | PIT를 이용한 지연 `wait 100[ms]` |
| `rdtsc` | 타임스탬프 카운터 읽기 |
| `cpuspeed` | 프로세서 속도 측정 |
| `date` | 현재 날짜·시간 출력 |
| `createtask` | 테스트 태스크 생성 후 컨텍스트 전환 데모 |

### 실행 환경

- **툴체인**: Cygwin(`make`, `nasm`, `gdb`) + `x86_64-pc-linux` 크로스 컴파일러
- **실행**: 번들된 QEMU 0.10.4 (`qemu-0.10.4/`)
- **디버깅**: 최신 QEMU + gdb 스텁 + VS Code C/C++ 확장 (F5)

```bash
make            # Bootloader + Kernel32 + Kernel64 + Utility + Disk.img
qemu-0.10.4/qemu-x86_64.bat   # 부팅
```

---

## 2. 프로젝트 구조

### 디렉터리 구성

```
10.OS/
├── 00.Bootloader/        16비트 부트로더 (NASM)
│   └── Bootloader.asm    BIOS가 0x7C00에 로딩, 커널 이미지를 디스크에서 적재
│
├── 01.Kernel32/          32비트 보호 모드 커널 (C + ASM)
│   └── src/
│       ├── EntryPoint.s  16→32비트 모드 전환 진입점
│       ├── main.c        메모리/CPU 확인, 64비트 커널 복사, IA-32e 전환
│       ├── page.c/.h     페이지 테이블 생성
│       ├── mode_sw.asm   IA-32e 모드 전환 어셈블리
│       └── types.h       기본 타입 정의
│
├── 02.Kernel64/          64비트 IA-32e 커널 (C + ASM) — 핵심 커널
│   └── src/
│       ├── EntryPoint.s        64비트 커널 진입점
│       ├── main.c              커널 초기화 및 부팅 시퀀스
│       ├── descriptor.c/.h     GDT · TSS · IDT 구성
│       ├── isr.asm             인터럽트 서비스 루틴(ISR) 스텁
│       ├── interrupt_handler.c 인터럽트/예외 핸들러
│       ├── pic.c/.h            PIC 컨트롤러 제어
│       ├── keyboard.c/.h       PS/2 키보드 인터페이스
│       ├── queue.c/.h          범용 큐 (키보드 버퍼 등)
│       ├── pit.c/.h            PIT (Programmable Interval Timer)
│       ├── rtc.c/.h            RTC (실시간 시계)
│       ├── console.c/.h        텍스트 화면 출력·커서
│       ├── console_shell.c/.h  콘솔 셸 (명령어 파싱/실행)
│       ├── task.c/.h           TCB·컨텍스트·스케줄러·fork
│       ├── memmap.c/.h         E820 파싱
│       ├── bootmem.c/.h        mem_map을 세우기 위한 부트스트랩 할당자
│       ├── pmm.c/.h            buddy 물리 프레임 할당자, page_t 관리
│       ├── paging.c/.h         4레벨 페이지 테이블, direct map, W^X
│       ├── slab.c/.h           slab + kmalloc/kfree
│       ├── vmalloc.c/.h        비연속 커널 매핑, guard page
│       ├── mm_struct.c/.h      프로세스 주소공간 (mm_t / vm_area_t)
│       ├── fault.c/.h          #PF 처리: 지연 할당, COW, SEGV
│       ├── elf.c/.h            ELF64 적재
│       ├── syscall.c/.h        int 0x80 디스패처
│       ├── user_app.c          Bin2C가 생성 - 임베드된 유저 ELF
│       ├── user_stub.asm       ring3 시험용 스텁들
│       ├── panic.c/.h          예외 덤프
│       ├── serial.c/.h         COM1 로깅
│       ├── mm.h / klist.h      공통 정의, 침습적 리스트
│       ├── utility.c/.h        메모리 함수, kPrintf 등
│       ├── assembly_utils.asm  포트 I/O, 인터럽트 제어, 컨텍스트 전환
│       └── types.h             기본 타입 정의
│
├── 03.Application/
│   └── 00.HelloWorld/    유저 프로그램. 커널 헤더를 쓰지 않고 따로 빌드된다
├── 04.Utility/
│   ├── 00.ImageMaker/    디스크 이미지 생성 도구
│   └── 01.Bin2C/         바이너리를 커널 .rodata 배열로 변환
│
├── tools/                QEMU 헤드리스 검증 하네스
│   ├── bootcheck.sh      부팅 후 셸 명령을 넣고 화면을 덤프
│   ├── regress.sh        빌드 + 부팅 회귀 (매 커밋)
│   └── verify_all.sh     전체 인수 검사 122개 (스테이지 경계)
│
├── qemu-0.10.4/          번들 QEMU (실행용)
├── ImageMaker.exe        빌드된 이미지 생성기
├── Disk.img             빌드 산출물 (1.44MB 플로피 이미지)
└── makefile             전체 빌드 오케스트레이션
```

### 빌드 파이프라인

```
hello.c ─gcc/ld─► hello.elf ─Bin2C─► user_app.c ┐
                                                 │  (커널보다 먼저 빌드된다)
Bootloader.asm ─nasm─► Bootloader.bin ┐          │
Kernel32 소스  ─gcc/ld─► Kernel32.bin  ├─ ImageMaker.exe ─► Disk.img ─► (1.44MB로 패딩)
Kernel64 소스  ─gcc/ld─► Kernel64.bin  ┘
```

유저 프로그램은 커널보다 **먼저** 빌드되어야 합니다. `Bin2C`가 만든
`user_app.c`가 커널 컴파일 대상에 들어가야 하기 때문입니다.

루트 `makefile`이 각 하위 디렉터리의 `make`를 순서대로 호출하고,
`ImageMaker.exe`가 세 바이너리를 하나의 부팅 이미지로 병합한 뒤
표준 플로피 크기(1,474,560바이트)로 패딩합니다.

### 3단계 커널 구조 이유

x86 CPU는 전원이 켜지면 **16비트 리얼 모드**로 시작합니다. 64비트
롱 모드로 곧바로 진입할 수 없기 때문에 단계적으로 전환합니다.

1. **Bootloader (16비트)** — 최소한의 코드로 나머지 커널 이미지를 메모리에 로딩
2. **Kernel32 (32비트 보호 모드)** — 메모리·CPU 확인, 페이지 테이블 준비 후 64비트 커널을 메모리에 복사
3. **Kernel64 (64비트 IA-32e)** — 실제 운영체제 기능이 동작하는 본체

---

## 3. 부팅 과정

CPU는 리얼 모드(16비트)로 부팅하므로 64비트 모드까지 3단계로 전환합니다.

```
① BIOS
   └─ 부트 섹터(512B)를 0x7C00에 로딩하고 실행

② Bootloader (16비트 리얼 모드)  — 00.Bootloader/Bootloader.asm
   ├─ 세그먼트/스택 초기화 (SS=0x0000, SP=0xFFFE)
   ├─ 화면 초기화 및 부팅 메시지 출력
   └─ 디스크에서 커널 이미지(Kernel32+Kernel64)를 0x10000에 로딩
       · TOTAL_SECTOR_SIZE, KERNEL32_SECTOR_SIZE 로 크기 관리
       └─ Kernel32 진입점으로 점프

③ Kernel32 (32비트 보호 모드)     — 01.Kernel32/src/main.c
   ├─ 최소 메모리 검사 (64MB 이상, kCheckMemSizeOvr64MB)
   ├─ IA-32e 커널 영역 초기화 (1MB~6MB를 0으로 클리어)
   ├─ IA-32e용 페이지 테이블 생성 (kInitPageTbl)
   ├─ CPUID로 프로세서 벤더 및 64비트 지원 여부 확인
   ├─ 64비트 커널(Kernel64)을 0x200000(2MB)로 복사 (kCpy64KernelToMem)
   └─ IA-32e 모드로 전환 후 64비트 커널로 점프 (kJump64BitKernel)

④ Kernel64 (64비트 IA-32e 모드)   — 02.Kernel64/src/main.c
   ├─ 콘솔 초기화
   ├─ GDT + TSS 구성 및 로드   (kInitGDTTableAndTSS / kLoadGDTR / kLoadTR)
   ├─ IDT 구성 및 로드         (kInitTDTTable / kLoadIDTR)
   ├─ 총 RAM 크기 확인         (kCheckTotalRAMSize)
   ├─ 키보드 인터페이스 활성화 (kInitializeKeyboard)
   ├─ PIC 초기화 · 인터럽트 활성화 (kInitializePIC / kEnableInterrupt)
   └─ 콘솔 셸 시작            (kStartConsoleShell)
```

### 커널 이미지가 메모리에 놓이는 순서

부트로더는 커널 전체 이미지를 `0x10000`에 연속으로 적재합니다. 이후
Kernel32가 자신보다 뒤쪽에 있는 Kernel64 부분만 떼어 `0x200000`으로
복사합니다. 복사 크기는 부트로더가 `0x7C05`(총 섹터 수), `0x7C07`
(Kernel32 섹터 수)에 기록해 둔 값으로 계산합니다.

```
0x10000 ┌────────────────────┐
        │ Kernel32 (32비트)  │  ← 여기서 실행
        ├────────────────────┤
        │ Kernel64 (64비트)  │  ── 복사 ──►  0x200000 (2MB)에서 실행
        └────────────────────┘
```

---

## 4. 메모리 구조

`add_multitask` 브랜치에서 메모리 관리를 리눅스식 모델로 옮겼습니다. 예전 문서에
있던 "고정 물리 주소 배치도"는 더 이상 시스템의 모습이 아닙니다 - 부팅 동안
잠깐 쓰이는 초기 배치일 뿐이고, 그 뒤로는 전부 할당자와 페이지 테이블이 정합니다.

### 4.1 무엇이 바뀌었나

| 항목 | 이전 | 현재 |
| --- | --- | --- |
| 메모리맵 획득 | 64MB부터 4MB씩 write probe | 실모드 E820 (`01.Kernel32/src/EntryPoint.s`) |
| 물리 프레임 할당 | 없음 (주소를 상수로 캐스팅) | buddy 할당자 `kAllocPages(order)` (`pmm.c`) |
| 프레임 메타데이터 | 없음 | `page_t` 배열 `mem_map` (`mm.h`) |
| 페이지 크기 | 2MB 고정 (PS=1) | 4KB 기본, direct map만 2MB |
| 커널 주소공간 | 0~64GB identity | higher-half: direct map + vmalloc + 커널 창 |
| VA↔PA 변환 | 없음 (같았으므로) | `__va()` / `__pa()` (`paging.h`) |
| 페이지 보호 | 전부 RW + supervisor + 실행가능 | `.text` RO+X, `.rodata` RO+NX, `.data` RW+NX, CR0.WP, NX |
| 커널 힙 | 없음 | slab + `kmalloc`/`kfree`, 비연속은 `vmalloc` |
| 프로세스 주소공간 | 없음 (CR3 하나) | `mm_t` + `vm_area_t`, 태스크마다 CR3 (`mm_struct.c`) |
| 페이지 폴트 | 벡터번호 출력 후 무한루프 | `kDoPageFault` → VMA 조회 → 지연 할당 / COW / SEGV (`fault.c`) |
| 지연 할당 | 없음 | demand paging, fork의 copy-on-write |
| 커널 스택 | 인덱스로 계산한 고정 8KB | 태스크마다 vmalloc 16KB + guard page |
| 유저/커널 분리 | 없음 (전부 ring0) | ring3 + `int 0x80`, U/S 경계 |

### 4.2 가상 주소 배치

커널은 고주소에 있고, 저주소 절반은 통째로 유저 몫입니다.

```
0xFFFFFFFF80200000  커널 이미지        PML4[511]  섹션별 4KB 권한
0xFFFFC90000000000  vmalloc 영역       PML4[402~] 태스크 커널 스택, guard page
0xFFFF800000000000  direct map         PML4[256]  물리 RAM 전체를 2MB로
-------------------  (비어 있음)
0x0000700000000000  유저 스택 꼭대기   PML4[224]
0x0000000000400000  유저 프로그램      PML4[0]    ELF PT_LOAD가 여기 앉는다
0x0000000000000000
```

`__va(pa)`는 `pa + PAGE_OFFSET`, `__pa(va)`는 고주소 커널 창과 direct map을 각각
되돌립니다. **커널 코드는 물리주소를 포인터로 역참조하지 않습니다** - 페이지 테이블
엔트리에 넣는 값만 물리주소이고, 그 테이블을 걷는 것조차 direct map을 거칩니다.

### 4.3 부팅 직후의 물리 배치

아래는 할당자가 서기 전까지만 유효합니다. 이 구간들은 `pmm.c`의 `kReserveRange`로
예약되고, 나머지 RAM은 전부 buddy 할당자가 가져갑니다.

| 주소 | 영역 | 비고 |
| --- | --- | --- |
| `0x00000` | 실모드 IVT/스택 | |
| `0x07C00` | 부트섹터 | |
| `0x07E00` | E820 테이블 | `bootinfo.h`, 실모드에서 수집 |
| `0x10000` | 커널 이미지 적재 | Kernel32+Kernel64 |
| `0x100000` | Kernel32 부트 페이지 테이블 | CR3 전환 후 **할당자에 반납** |
| `0x142000` | GDT/TSS/IDT | 한 페이지 |
| `0x200000` | Kernel64 이미지 | VMA는 `0xFFFFFFFF80200000` |
| `0x600000` | 부트 스택 | RSP는 direct map 별칭 |
| `0x700000` | IST1 스택 | |
| `0xB8000` | VGA 텍스트 버퍼 | `__va()`로 접근 |

예전 문서의 `0x800000` 태스크 풀 / 스택 풀은 **더 이상 없습니다.** TCB 풀은
`kAllocPages`로, 태스크 스택은 `kVmapPages`로 받습니다.

### 4.4 태스크 컨텍스트(TCB) 구조

```c
typedef struct kTaskControlBlockStruct {
    ListLink_t stLink;       // 반드시 첫 멤버 (list.c가 그대로 캐스팅한다)
    QWORD      qwFlag;       // TASK_FLAG_DEAD 등
    Context_t  tContext;     // 레지스터 24개
    void*      pvStackAddr;  // 커널 스택 (vmalloc, guard page 포함)
    QWORD      qwStackSize;
    mm_t*      poMM;         // 주소공간. 커널 스레드는 NULL
    QWORD      qwCR3;        // poMM->qwPML4. 커널 스레드는 0
} TCB_t;
```

컨텍스트 전환은 `kSwitchContext(현재, 다음)`이 하고, 그 직전에 스케줄러가
CR3와 `TSS.rsp0`를 다음 태스크 것으로 맞춥니다. 커널 스레드(`qwCR3 == 0`)는
CR3를 바꾸지 않고 현재 주소공간을 빌려 씁니다 - 커널 절반이 모든 주소공간에서
같으므로 안전하고, 전환마다 TLB를 비우지 않아도 됩니다(리눅스의 lazy TLB).

### 4.5 확인용 셸 명령

| 명령 | 확인 내용 |
| --- | --- |
| `memmap` / `totalram` | E820 파싱 결과 |
| `pmemstat` / `alloctest` / `frameinfo` | buddy 할당자 |
| `pgwalk <va>` / `pgtest` / `maptest <va> [user]` | 페이지 테이블, W^X, US 전파 |
| `slabinfo` / `kmalloctest` / `vmalloctest` | 커널 힙 |
| `mmtest` / `cr3test` | 주소공간 생성·전환 |
| `usertest [bad\|demand\|fork]` | ring3, SEGV, 지연 할당, COW |
| `exec` | 임베드된 ELF64를 적재해 ring3에서 실행 |

전체 인수 검사는 `tools/verify_all.sh`입니다(현재 122개 항목).
